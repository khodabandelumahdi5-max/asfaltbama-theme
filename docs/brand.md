# هویت برند — قطعه فوری / GetFori

## نام

| | |
|---|---|
| **نام فارسی** | قطعه فوری (نسخه جایگزین «گت فوری» — نام فارسی همان «عنوان سایت» است و با یک تغییر در تنظیمات عوض می‌شود) |
| **نام لاتین** | GetFori — ترکیب «Get» (بگیر) و «فوری»؛ تلفظ: گِت‌فوری |
| **دامنه اصلی** | `ghetehfori.ir` — طبق اعلام مالک در ۱۴۰۵/۰۷/۰۳ در اختیار است (از این محیط با WHOIS ایرنیک تأیید نشد) |
| **دامنه‌های .com** | `ghetehfori.com`، `getfori.com`، `ghetefori.com`: طبق Verisign RDAP در ۱۴۰۵/۰۷/۰۳ **ثبت‌نشده** بودند. پیشنهاد: `getfori.com` را هم‌زمان ثبت کنید (حفاظت برند و آینده بین‌المللی). |
| **نکته املای دامنه** | «ghetehfori» شکل رایج فینگلیش «قطعه فوری» است، ولی کاربران «ghatefori» یا «ghate-fori» هم تایپ می‌کنند. در صورت امکان این املاها را هم ثبت و به دامنه اصلی ریدایرکت کنید. |
| **علامت تجاری** | **NEEDS VALIDATION**: قبل از چاپ تابلو و بسته‌بندی، نام را در سامانه مالکیت معنوی (ثبت علائم تجاری) جستجو و در طبقه ۱۲ (قطعات وسایل نقلیه) و ۳۵ (فروش) ثبت کنید. این بررسی حقوقی نیست. |

## وعده برند (Brand Promise)

**«قطعه درست، همین حالا.»**

«فوری» یک قول است و باید در عمل هم پایش ایستاد؛ وگرنه به ضد اعتماد تبدیل می‌شود. پیشنهاد اجرایی قبل از تبلیغ این قول:

- ارسال همان‌روز در شهر مبدأ برای سفارش‌های قبل از ساعت مشخص.
- نمایش «ارسال فوری» فقط روی کالاهای موجود در انبار.
- داشبورد «سفارش‌هایی که منتظر شما هستند» (قرمز بعد از ۲۴ ساعت) برای کنترل همین قول است.

## جایگاه (Positioning)

برای **مکانیک‌ها، تعمیرگاه‌ها، فروشندگان و راننده‌هایی** که قطعه **برقی، بدنه و چراغ یا مکانیکی** خودروهای **ژاپنی، چینی، کره‌ای و ایرانی** را **سریع و درست** لازم دارند، «قطعه فوری» با **شماره فنی دقیق، انتخاب بر اساس خودرو و ارسال فوری**، حدس و رفت‌وآمد بازار را حذف می‌کند.

## شخصیت و لحن

| هستیم | نیستیم |
|---|---|
| فنی و دقیق (شماره فنی، سال ساخت، سازگاری) | پرادعا و تبلیغاتی |
| سریع و رک | عجول و بی‌دقت |
| همکار تعمیرکار | فروشنده‌ای که فقط می‌فروشد |

- جمله‌ها کوتاه. عدد و مشخصات به‌جای صفت («۶ ماه ضمانت» نه «بهترین کیفیت»).
- مخاطب را «شما» خطاب کنید. با همکاران B2B رسمی ولی گرم.

## لوگو

- **نشانه:** یک چراغ (نیم‌دایره نارنجی) با سه پرتو نور سفید که با طول‌های کم‌شونده حس سرعت می‌دهند. کل شکل حرف **F** (فوری/Fori) را هم تداعی می‌کند. بدون چرخ‌دنده و آچار کلیشه‌ای.
- **فایل‌ها:** `wordpress/themes/yadak-child/assets/brand/`

| فایل | کاربرد |
|---|---|
| `mark.svg` | نشانه؛ فاوآیکن، آواتار شبکه‌های اجتماعی، اپ |
| `logo-horizontal.svg` | لوگوی کامل (نشانه + نام فارسی + GETFORI) برای چاپ و فاکتور |
| `logo-512.png` | لوگوی رستری برای گوگل (Organization schema) |
| `og-cover.png` | تصویر اشتراک‌گذاری ۱۲۰۰×۶۳۰ در تلگرام، واتس‌اپ و اینستاگرام |

- **حریم:** دور لوگو حداقل به اندازه ارتفاع یک پرتو نور فضای خالی بماند.
- **حداقل اندازه نشانه:** ۱۶ پیکسل (فاوآیکن)، در چاپ ۸ میلی‌متر.
- روی پس‌زمینه تیره، نشانه با همان مربع سرمه‌ای و یک خط دور ۱ پیکسلی روشن استفاده شود.
- **نکته:** لوگوی فعلی طراحی اولیه و آماده استفاده است. برای تابلو، بسته‌بندی و ثبت علامت تجاری، بهتر است یک طراح گرافیک آن را نهایی کند (نسخه تک‌رنگ، نسخه مهر و …).

## رنگ

| نقش | رنگ | کد |
|---|---|---|
| سرمه‌ای اصلی (اعتماد، فنی) | ■ | `#0F172A` |
| سرمه‌ای دوم | ■ | `#1E293B` |
| نارنجی فوری (اقدام، نور) | ■ | `#EA580C` |
| نارنجی تیره (hover) | ■ | `#C2410C` |
| سطح روشن | ■ | `#F5F7FA` |
| متن کم‌رنگ | ■ | `#64748B` |
| موفق / موجود | ■ | `#15803D` |

قانون: نارنجی فقط برای **اقدام** (دکمه خرید، انتخاب خودرو، هایلایت قطعه) است، نه برای تزئین.

## تایپوگرافی

- **وزیرمتن (Vazirmatn)**، فونت متغیر روی خود سایت (مجوز OFL). تیتر ۹۰۰، زیرتیتر ۷۰۰، متن ۴۰۰ تا ۵۰۰.
- اعداد متن فارسی با ارقام فارسی. **شماره فنی، کد کالا و تلفن با ارقام لاتین و چپ‌به‌راست**، تا با کاتالوگ سازنده یکی باشد و کپی و جستجویش درست کار کند.

## کاربردها (فاز بعد)

جعبه ارسال (سرمه‌ای، نشانه نارنجی، شعار)، برچسب اصالت با QR به صفحه محصول، فاکتور (قالب چاپ فعلی سایت)، کارت ویزیت، لباس پیک، و هایلایت‌های اینستاگرام: برقی / بدنه و چراغ / مکانیکی / ارسال / همکاران.
